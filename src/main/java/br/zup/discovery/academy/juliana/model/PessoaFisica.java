package br.zup.discovery.academy.juliana.model;

    public class PessoaFisica extends Account {
        public PessoaFisica(long id, long number, double balance, Cliente cliente) {
            super(id, number, balance, cliente);
        }
    }
